pacman::p_load(funModeling, tidyverse) 

glimpse(colon) # olhada nos dados
status(colon) # estrutura dos dados (missing etc)
freq(colon) # frequência das variáveis fator
plot_num(colon) # exploração das variáveis numéricas
profiling_num(colon) # estatísticas das variáveis numéricas
